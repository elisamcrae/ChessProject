package services;

import requests.RegisterRequest;
import responses.RegisterResponse;

public class RegisterService {

    /**
     * Responds to an HTTP request to add a new user into the database
     * @param r the HTTP request to register a user
     * @return  the HTTP response to the register user request
     */
    RegisterResponse register(RegisterRequest r) {
        return null;
    }
}
